apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-tenjin-pulse-vts
  labels:
    app: {{ .Release.Name }}-tenjin-pulse-vts
spec:
  type: {{ .Values.jmeterVts.service.type | default "ClusterIP" }}
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: {{ .Release.Name }}-tenjin-pulse-vts
